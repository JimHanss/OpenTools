# 项目进度

## 当前状态

**已完成 — Web Mind Map MVP。** 已于 2026-07-15 通过验证：格式检查、lint、typecheck、63 项单元/集成测试、生产构建，以及 4 项 Chromium 端到端测试均成功。功能规格见 [web-mind-map-mvp](specs/web-mind-map-mvp/spec.md)，详细证据见 [verify.md](specs/web-mind-map-mvp/verify.md)。

## 已完成功能

- 本地优先的导图库，支持创建、打开、重命名、复制、确认后删除，以及独立导入 JSON。
- 基于 Command 的编辑器会话，支持撤销/重做、防抖自动保存、刷新恢复和用户可见的保存状态。
- 支持键盘/鼠标主题编辑、多选、剪贴板降级方案、折叠/展开、拖放、平移/缩放、适应画布、居中选中项和搜索。
- 支持主题样式、标记、备注、安全链接、关系线、边界、概要，以及可撤销的整理顺序预览与执行。
- 支持内部 JSON v2 解析、迁移和导出；支持完整导图 SVG 和 PNG 导出，并在失败时安全降级为 SVG。
- 支持根据浏览器语言自动选择简体中文或英文、手动切换并持久化语言偏好；切换语言不会修改导图内容。
- 提供平台无关的领域模型、Command、布局、SVG scene、格式和存储边界，便于后续接入小程序或移动端 adapter。

## 进行中

当前没有正在进行的实现任务。下一项产品决策是启动响应式/PWA 优化，还是开发小程序平台 adapter；两者均不属于已完成的 Web MVP 范围。

## 已知风险

- 导图存储在浏览器本地。清除站点数据、切换浏览器 profile 或存储故障都可能导致数据丢失；应通过 JSON 导出进行备份。
- 当前只提供从左到右布局，可编辑文件格式为 OpenTools JSON v2，不是 XMind 或 OPML。
- Chromium 已由浏览器自动化测试覆盖。在公开发布候选版本前，仍需使用当前版本 Edge 完成一次人工 smoke test。

## 最后更新

2026-07-15 09:55 Asia/Shanghai — 中英文界面和语言偏好持久化已纳入验证；完整检查重新通过。
